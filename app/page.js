import Hero from "./components/Hero";
import ScrollProgress from "./components/ScrollProgress";

const page = () => {
    return (
        <main>
            <ScrollProgress />
            <Hero />
        </main>
    );
};
export default page;
